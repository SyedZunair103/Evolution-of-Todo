/** @type {import('next').NextConfig} */
const nextConfig = {
  // Correct Turbopack configuration
  experimental: {
    // Empty object to enable Turbopack without issues
  },
};

module.exports = nextConfig;